apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: boutique-services
  namespace: monitoring
  labels:
    release: prometheus
spec:
  namespaceSelector:
    matchNames:
      - boutique
  selector:
    matchLabels: {}
  endpoints:
    - port: http
      interval: 15s
      path: /metrics
